# 주간 Todo List 웹앱 — 기획서 (v1)

## 1. 개요
- **목적**: 개인용 주간 할 일 관리 웹앱
- **핵심 컨셉**: 좌측 "할 일 보관함(Todo List)" + Mon~Sun 7개 요일 컬럼, 총 8칸 레이아웃
- **참고**: 첨부된 노션 타임 플래너 이미지의 레이아웃(할 일 목록 + 요일별 컬럼)을 웹앱으로 재현
- **사용 환경**: 여러 기기(PC, 폰 등)에서 로그인 후 동기화하여 사용

> 확정: **Todo List(1) + Mon~Sun(7) = 8칸**, 토·일 포함.

## 2. 핵심 기능 (MVP)
1. **Todo 등록**: 좌측 Todo List 칸에서 할 일을 자유롭게 추가/수정/삭제
2. **드래그 앤 드롭 배치**: Todo List의 항목을 Mon~Sun 중 원하는 요일 컬럼으로 드래그하여 이동
   - 요일 ↔ 요일 간 재배치 가능
   - 요일 → Todo List로 되돌리기도 가능
   - 같은 컬럼 내 순서 변경 가능
3. **완료 체크**: 각 할 일 항목에 체크박스 → 체크 시 완료 표시(취소선 등)
4. **주차 이동**: 상단에 `< 52주 >` 형태의 주차 네비게이션. 좌/우 화살표로 이전 주 / 다음 주로 이동하면 Mon~Sun 7칸의 내용이 해당 주 것으로 바뀜
5. **자동 저장 & 동기화**: 어느 기기에서 수정하든 로그인 계정 기준으로 실시간 반영

MVP 범위에는 포함하지 않지만 구조상 나중에 쉽게 얹을 수 있는 것들은 6번에 따로 정리했습니다.

## 3. 화면 구성 (MVP)
- 심플한 헤더(앱 이름 정도)
- 주차 네비게이션: `<  52주  >` — 화살표 클릭으로 이전/다음 주 이동, 가운데에 현재 보고 있는 주차 번호(ISO 기준) 표시
- 8칸 보드: `[Todo List] [Mon] [Tue] [Wed] [Thu] [Fri] [Sat] [Sun]`
  - **Todo List 칸은 주차와 무관한 전역 보관함**입니다. 아직 요일을 정하지 않은 할 일을 아무 때나 적어두는 곳이라, 주차를 이동해도 내용이 바뀌지 않습니다.
  - **Mon~Sun 7칸은 현재 보고 있는 주차 전용**입니다. 주차를 이동하면 이 7칸의 내용만 그 주 데이터로 교체됩니다.
  - (이 부분은 제 판단으로 정한 가정입니다 — Todo List 보관함도 주차별로 따로 관리하고 싶으시면 말씀해주세요.)
- 이미지에 있는 quarterly/monthly/daily 탭, 상단 진행률 바 등은 이번 범위에서 제외 (향후 확장 아이디어로 분류)

## 4. 기술 스택 (결정: 멀티 디바이스 동기화, 개인용, 완전 무료)
| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | Next.js + TypeScript | 무료 배포(Vercel)와 궁합이 가장 좋음 |
| 스타일 / UI 컴포넌트 | Tailwind CSS + shadcn/ui | 체크박스, 버튼, 카드, input 등 기본 컴포넌트를 shadcn/ui로 구성해 빠르고 깔끔하게 UI 완성 |
| 드래그 앤 드롭 | dnd-kit | 활발히 유지보수되는 React DnD 라이브러리, 접근성 지원 |
| 백엔드/DB | Supabase (Postgres + Auth) | 무료 티어로 DB + 로그인 인증까지 한 번에 해결 |
| 인증 | Supabase Auth (이메일/비밀번호 또는 매직링크) | 1인용이라 최소한의 로그인만 필요 |
| 호스팅 | Vercel (Hobby 무료 플랜) | Next.js 배포 표준, 개인 프로젝트 트래픽엔 무료로 충분 |

**비용**: Vercel Hobby(무료) + Supabase Free tier(무료, DB 500MB / 월간 API 요청 한도 넉넉함) 조합으로 개인용은 완전 무료로 운영 가능합니다.

## 5. 데이터 모델 (초안)
`todos` 테이블
| 필드 | 타입 | 설명 |
|---|---|---|
| id | uuid | PK |
| user_id | uuid | Supabase Auth 사용자 (RLS로 본인 데이터만 접근) |
| content | text | 할 일 내용 |
| day | text (nullable) | `null`(Todo List 전역 보관함) 또는 `mon`~`sun` |
| week_start_date | date (nullable) | `day`가 `mon`~`sun`일 때만 값 있음 — 해당 요일이 속한 주의 월요일 날짜. 이 값으로 "몇 주차인지" 계산 및 필터링 |
| completed | boolean | 완료 여부 (기본 false) |
| position | int | 같은 칸 내 정렬 순서 |
| created_at | timestamptz | 생성 시각 |

- 주차 네비게이션에서 보여줄 "52주" 같은 주차 번호는 `week_start_date`로부터 ISO 주차를 계산해서 표시(별도 저장 안 함).
- Row Level Security로 `user_id = auth.uid()` 조건을 걸어 본인 데이터만 읽기/쓰기 가능하도록 구성합니다.

## 6. 향후 확장 아이디어 (MVP 이후, 지금은 안 함)
- ~~시간 단위 입력 및 표시~~ — **완료 (2026-09-11)**. Mon~Sun이 시간축 있는 캘린더 그리드로 바뀌었고,
  할 일을 드래그로 특정 시간에 배치 + 블록 리사이즈로 소요 시간 조절 가능. 자세한 내용은
  [HANDOFF.md](./HANDOFF.md)의 결정 흐름 8번 참고.
- 카테고리/색상 태그 (콘텐츠·회사·루틴·개인 등)
- 월간/분기 플래너 탭 확장
- ~~"이번 주로 돌아가기" 바로가기 버튼~~ — 완료 (주차 네비게이션의 "이번 주" 버튼)
- 같은 시간대에 여러 할 일이 겹칠 때 나란히 배치하는 레이아웃 (지금은 겹쳐서 보임)

## 7. 진행 순서
1. ✅ 기획서 확정
2. ✅ 프로젝트 초기 세팅 (Next.js + Tailwind + dnd-kit)
3. ✅ 정적 UI 레이아웃 구현 (8칸 보드)
4. ✅ Todo 추가/수정/삭제 (로컬 상태 → Supabase로 교체 완료)
5. ✅ 드래그 앤 드롭 구현
6. ✅ Supabase 연동 (이메일/비밀번호 로그인 + 데이터 저장/실시간 동기화, RLS 적용) — 설정 방법은 [README.md](./README.md#supabase-설정-최초-1회) 참고
7. ✅ Vercel 배포 — https://plan0.vercel.app

## 8. PARA 확장 기획 (Project / Area / Resource) — (2026-09-12)

> **개념(8.1~8.3)은 사용자 확인 완료.** 데이터 모델(8.4)/화면 설계(8.5)는 아직 초안 단계이고,
> 이 프로젝트의 작업 방식 규칙(HANDOFF.md 참고)에 따라 **실제 코드 작업 전에 HTML 목업이나
> Figma로 화면을 먼저 만들어 사용자 컨펌을 받아야** 착수합니다. 아래 8.4/8.5는 그 목업을 만들기
> 위한 참고용 초안이지 확정된 설계가 아닙니다.

### 8.1 목적

지금까지의 "요일·시간 캘린더"는 **언제 할지**를 답하는 축입니다. 여기에 **무엇을 위해 하는지**를 답하는
두 번째 축으로 [PARA](https://fortelabs.com/blog/para/) (Project / Area / Resource / Archive) 방법론을
얹습니다. 두 축은 서로 독립적으로 동작합니다 — 어떤 할 일이 특정 프로젝트에 속해 있어도, 요일/시간
캘린더 배치는 그것과 무관하게 계속 할 수 있고 그 반대도 마찬가지입니다. (확정)

### 8.2 이 앱에서의 PARA 개념 정의

**Project / Area / Resource는 계층 구조가 아니라 완전히 독립된 3개의 병렬 분류 체계입니다.**
("Area가 Project를 묶는다"는 이전 초안의 가정은 폐기 — 사용자 확인.) 셋 다 "이름을 가진 여러 개의
컨테이너를 만들 수 있고, 그 컨테이너에 할 일을 드래그해서 매핑한다"는 동일한 동작 방식을 공유하되,
의미만 다릅니다:

- **Project(프로젝트)**: **시작과 끝이 있는 일.** 예: "6월 한 달 동안 영어 강의 완강". 상태는 `진행중` /
  `완료` 정도로 최소화 (세분화는 향후 확장).
- **Area(영역)**: **시작과 끝이 없이 계속 해야 하는 일들.** 예: "영어". Project와 동일하게 할 일을
  직접 드래그해서 매핑받습니다 (확정).
- **Resource(리소스)**: **지금 당장은 신경 쓰지 않지만 관심 있는 것들의 모음.** 예: "일본어" (지금
  집중하는 건 아니지만 관심은 있는 것). Project/Area와 동일하게 할 일을 드래그해서 보낼 수 있습니다
  (확정). 기존 `/api/clip` 스크랩 기능(현재는 Todo List로 들어감)과 개념적으로 겹치는 지점이 있어
  향후 연결을 고려할 수 있습니다 (열린 질문 참고).

  > 같은 "언어 공부"라는 주제도 지금 기간을 정해 몰입 중이면 Project("영어 강의 완강"), 꾸준히
  > 유지만 하면 되는 거면 Area("영어"), 아직 시작 안 했지만 관심만 있으면 Resource("일본어")로
  > 갈리는 식입니다 — 주제가 아니라 **지금 이 일에 쏟는 주의/시간의 성격**이 분류 기준입니다.

- **컨테이너(바구니/폴더) 비유**: Project/Area/Resource 각각은 폴더나 바구니 같은 것이라, 인스턴스
  하나(예: Area "영어")에 할 일을 몇 개든 담을 수 있습니다(0..N, 이미 데이터 모델에 반영됨 — 8.4 참고).
  다만 할 일 하나는 여러 바구니에 동시에 담기지 않고 최대 1곳에만 들어갑니다(8.3 확정 사항, 변경 없음).
- **Archive**: 이번 기획 범위에서 제외합니다. Project/Area/Resource는 "완료"/"보관" 정도의 상태만
  갖고, 별도의 보관함 화면은 나중에 필요해지면 추가합니다.

### 8.3 관계 구조 (확정)

```
Project (이름을 가진 컨테이너, 0..N개)
Area    (이름을 가진 컨테이너, 0..N개)     ── 서로 완전히 독립, 계층 관계 없음
Resource(이름을 가진 컨테이너, 0..N개)

Todo ──드래그로 매핑── 위 세 종류 중 구체적인 컨테이너 인스턴스 하나
```

- Project, Area, Resource는 서로 완전히 독립적인 top-level 목록입니다. 하나가 다른 하나를 담거나
  소속되는 관계는 없습니다.
- 할 일 1개는 이 셋(정확히는 셋 중 하나의 구체적 인스턴스)을 통틀어 **최대 1곳에만** 속합니다.
  둘 이상 동시에 속하지 않습니다 — 예를 들어 Project "이사 준비"와 Area "건강"에 동시에 매핑될 수
  없습니다. 아무 데도 안 속하면 기존처럼 "미배정" 상태(Todo List 보관함에만 존재).
- 요일/시간 캘린더 배치는 이 분류와 완전히 독립적인 축으로, 변경 없이 계속 별개로 동작합니다.

### 8.4 데이터 모델 변경안 (초안 — 목업 컨펌 전까지 미확정)

새 테이블 (세 테이블은 같은 모양, 계층 관계 없음):

| 테이블 | 필드 | 설명 |
|---|---|---|
| `projects` | id, user_id, name, status (`active`\|`completed`), created_at, completed_at | 프로젝트 |
| `areas` | id, user_id, name, archived (bool), created_at | 영역 |
| `resources` | id, user_id, name, archived (bool), created_at | 리소스 |

기존 `todos` 테이블에 컬럼 추가:

| 필드 | 타입 | 설명 |
|---|---|---|
| `project_id` | uuid, nullable FK→projects | |
| `area_id` | uuid, nullable FK→areas | |
| `resource_id` | uuid, nullable FK→resources | |

- 셋 중 최대 1개만 값이 있어야 합니다 — DB에 `CHECK (num_nonnulls(project_id, area_id, resource_id) <= 1)`
  제약을 걸어서 강제합니다 (Postgres `num_nonnulls` 함수 사용).
- `day`/`week_start_date`/`start_minutes`/`duration_minutes`는 그대로 둡니다 — PARA 축과 무관하게
  유지 (확정).

### 8.5 화면/UX 설계 (초안 — 다음 단계는 코드가 아니라 HTML/Figma 목업)

- **완전히 별도의 화면**으로 둡니다 (확정). 기존 아이콘 레일에 새 아이콘(예: 폴더 아이콘)을 추가해서
  누르면 `/para` 같은 새 라우트로 이동.
- 화면 안에 **[Project] [Area] [Resource] 탭(또는 3열 컬럼)** + **할 일 보관함**을 함께 배치해서
  드래그 앤 드롭이 한 화면에서 성립하도록 합니다:
  - 할 일 보관함: 기존 Todo List 전역 보관함과 같은 데이터(미배정 포함, 이미 매핑된 항목도 어느
    컨테이너에 속해 있는지 배지로 표시).
  - 탭/컬럼별로 그 종류(Project든 Area든 Resource든)에 속한 컨테이너 카드 목록이 보임.
  - 할 일 카드를 특정 컨테이너 카드 위로 드래그하면 그 컨테이너로 매핑(`project_id`/`area_id`/
    `resource_id` 중 해당 필드 설정, 나머지 둘은 자동으로 비움). 다시 보관함으로 드래그하면 매핑 해제.
- 컨테이너(Project/Area/Resource)를 클릭하면 상세 화면(그 안에 매핑된 할 일 목록, 완료/미완료 구분)
  으로 이동.
- Project/Area/Resource 생성은 각각 이름 입력 폼 하나로 최소화 (기존 "할 일 추가" 폼과 비슷한 패턴).
- 메인 캘린더 화면의 할 일 카드에 소속 컨테이너 배지를 보여줄지는 향후 판단 — 요일/우선순위용으로
  이미 색상을 아껴두기로 한 기존 결정(HANDOFF.md 6번 참고)과 안 겹치게, 색상 대신 작은 텍스트
  라벨/아이콘 방식을 우선 고려.

#### 8.5.1 Project 상세 화면 스펙 (레퍼런스: Notion 프로젝트 페이지 캡처, 2026-09-12)

사용자가 첨부한 Notion 프로젝트 페이지 캡처를 레퍼런스로 **Project 상세 화면**만 우선 스펙을 잡습니다.
(Area/Resource 상세 화면도 같은 레이아웃을 쓸지는 8.5.1 끝의 열린 질문 참고 — 아직 미확정.)

**구성 (위에서 아래 순서):**

1. **헤더**: 아이콘(이모지 1개, 레퍼런스의 🚀처럼) + 제목(프로젝트 이름)
2. **요약 속성 줄** (헤더 바로 아래, 가로 나열): `Status`(진행중/완료) · `Due date` · `Progress`(이
   프로젝트에 속한 할 일 완료 비율 %, 자동 계산 — 저장 안 함)
   - 레퍼런스에 있던 **`Priority`는 제외** (확정 — 사용자 지시, "아직 필요 없음")
3. **탭**: **Overview / Tasks / Notes** 3개만 (확정 — 레퍼런스의 `Ideas`, `Meeting Notes` 탭은 제외)
   - **Overview 탭**: Properties 목록을 보여줌 — 레퍼런스처럼 라벨-값 형태의 세로 리스트
   - **Tasks 탭**: 이 프로젝트에 매핑된 할 일 목록 (레퍼런스는 "Table view"였지만, 우리 앱은 기존
     Todo 카드 스타일 리스트로 보여주는 걸 우선 고려 — 아래 열린 질문 참고)
   - **Notes 탭**: 프로젝트 전체에 대한 자유 텍스트 메모 (개별 할 일의 메모와는 별개)

**Overview 탭의 Properties 후보** (레퍼런스에서 이 앱 개념에 맞게 추려본 초안 — 전부 확인 필요):

| 레퍼런스 필드 | 이 앱에 적용한다면 | 상태 |
|---|---|---|
| Type (Business) | 해당 없음(개인용 앱이라 이런 분류 불필요해 보임) | 제외 제안 — 확인 필요 |
| Start date | 프로젝트 시작일 | 추가할지 확인 필요 (지금 데이터 모델엔 없음, `created_at`으로 대체 가능) |
| Completion date | 완료 처리된 날짜(완료 전엔 비어있음) | 추가할지 확인 필요 (지금은 `completed_at`만 있음 — 같은 개념일 수 있음) |
| Days left | `Due date` 기준 자동 계산 | `Due date` 자체를 추가하기로 하면 자연히 따라옴 |
| Tasks progress | 요약 줄의 `Progress`와 같은 값 | 여기서도 중복 표시할지, 요약 줄에만 둘지 확인 필요 |
| Notes | (본문 텍스트) | Notes를 별도 탭으로 뺐으니 이 property 줄은 빼는 게 자연스러워 보임 — 확인 필요 |
| "12 more properties" (접기/펼치기) | — | 이 앱은 필드 수가 적어서 이번엔 불필요해 보임 — 확인 필요 |

**열린 질문 (사용자 확인 필요):**

1. `Due date`(목표 마감일)를 Project에 실제로 추가할까요? 지금 데이터 모델(8.4)엔 없는 개념입니다.
2. `Start date`를 별도로 저장할까요, 아니면 `created_at`(만든 날짜)을 그대로 "시작일"로 보여줄까요?
3. `Completion date`는 지금 설계된 `completed_at`(완료 처리 시각)과 같은 걸로 봐도 될까요?
4. Tasks 탭은 표(Table) 형태로 만들까요, 기존 앱의 Todo 카드 리스트 스타일을 그대로 쓸까요?
5. 이 Overview/Tasks/Notes 레이아웃을 **Area/Resource 상세 화면에도 그대로 쓸까요**, 아니면
   Area는 "끝이 없는 일"이라 `Due date`/`Days left` 같은 속성이 안 맞으니 Area/Resource는 더 단순한
   자체 레이아웃(예: Overview 없이 Tasks + Notes만)을 쓸까요?

### 8.6 진행 순서 (제안, 아직 미착수)

1. 데이터 모델 확정 + `supabase/schema.sql`에 `projects`/`areas`/`resources` 테이블과 `todos` 컬럼(3개
   nullable FK + CHECK 제약) 추가, RLS 정책 작성
2. `/para` 라우트 + Project/Area/Resource 탭(또는 3열) 정적 목록 UI
3. Project/Area/Resource 생성 폼
4. 할 일 보관함 ↔ 컨테이너 카드 드래그 앤 드롭 매핑 (dnd-kit 재사용, 셋 중 하나로만 배타적으로 매핑)
5. 컨테이너 상세 화면 (매핑된 할 일 목록)
6. (선택, 이후) 메인 캘린더 화면에 소속 컨테이너 배지 표시

### 8.7 열린 질문 (사용자 확인 필요)

1. Project/Area/Resource의 "완료"/"보관" 상태로 처리하면 화면에서 어떻게 되나요 — 목록에서 사라지나요,
   접혀서 따로 보이나요, 별도 탭이 필요한가요?
2. Resource와 기존 `/api/clip` 스크랩 기능(현재 Todo List로 들어감)을 이번에 연결할까요 — 예를 들어
   스크랩된 링크가 특정 Resource로 바로 들어가게 할지, 아니면 이번엔 완전히 별개로 두고 나중에 고민할까요?
3. 메인 캘린더 화면의 할 일 카드에 소속 컨테이너를 표시할지(배지/라벨), 아니면 이번 범위는 `/para`
   화면 안에서만 확인하는 걸로 충분한가요?
4. Project/Area/Resource 탭 UI는 3열을 한 화면에 동시에 보여줄지, 탭으로 하나씩 전환할지 — 모바일
   화면 폭을 고려하면 탭 전환이 유력해 보이는데 맞을까요?
