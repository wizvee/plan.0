# 주간 Todo List 웹앱 — 기획서 (v1)

## 1. 개요
- **목적**: 개인용 주간 할 일 관리 웹앱
- **핵심 컨셉**: 좌측 "할 일 보관함(Todo List)" + Mon~Sun 7개 요일 컬럼, 총 8칸 레이아웃
- **참고**: 첨부된 노션 타임 플래너 이미지의 레이아웃(할 일 목록 + 요일별 컬럼)을 웹앱으로 재현
- **사용 환경**: 여러 기기(PC, 폰 등)에서 로그인 후 동기화하여 사용

> 확정: **Todo List(1) + Mon~Sun(7) = 8칸**, 토·일 포함.

## 2. 핵심 기능 (MVP)
1. **Todo 등록**: 좌측 Todo List 칸에서 할 일을 자유롭게 추가/수정/삭제
2. **드래그 앤 드롭 배치**: Todo List의 항목을 Mon~Sun 중 원하는 요일 컬럼으로 드래그하여 이동
   - 요일 ↔ 요일 간 재배치 가능
   - 요일 → Todo List로 되돌리기도 가능
   - 같은 컬럼 내 순서 변경 가능
3. **완료 체크**: 각 할 일 항목에 체크박스 → 체크 시 완료 표시(취소선 등)
4. **주차 이동**: 상단에 `< 52주 >` 형태의 주차 네비게이션. 좌/우 화살표로 이전 주 / 다음 주로 이동하면 Mon~Sun 7칸의 내용이 해당 주 것으로 바뀜
5. **자동 저장 & 동기화**: 어느 기기에서 수정하든 로그인 계정 기준으로 실시간 반영

MVP 범위에는 포함하지 않지만 구조상 나중에 쉽게 얹을 수 있는 것들은 6번에 따로 정리했습니다.

## 3. 화면 구성 (MVP)
- 심플한 헤더(앱 이름 정도)
- 주차 네비게이션: `<  52주  >` — 화살표 클릭으로 이전/다음 주 이동, 가운데에 현재 보고 있는 주차 번호(ISO 기준) 표시
- 8칸 보드: `[Todo List] [Mon] [Tue] [Wed] [Thu] [Fri] [Sat] [Sun]`
  - **Todo List 칸은 주차와 무관한 전역 보관함**입니다. 아직 요일을 정하지 않은 할 일을 아무 때나 적어두는 곳이라, 주차를 이동해도 내용이 바뀌지 않습니다.
  - **Mon~Sun 7칸은 현재 보고 있는 주차 전용**입니다. 주차를 이동하면 이 7칸의 내용만 그 주 데이터로 교체됩니다.
  - (이 부분은 제 판단으로 정한 가정입니다 — Todo List 보관함도 주차별로 따로 관리하고 싶으시면 말씀해주세요.)
- 이미지에 있는 quarterly/monthly/daily 탭, 상단 진행률 바 등은 이번 범위에서 제외 (향후 확장 아이디어로 분류)

## 4. 기술 스택 (결정: 멀티 디바이스 동기화, 개인용, 완전 무료)
| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | Next.js + TypeScript | 무료 배포(Vercel)와 궁합이 가장 좋음 |
| 스타일 / UI 컴포넌트 | Tailwind CSS + shadcn/ui | 체크박스, 버튼, 카드, input 등 기본 컴포넌트를 shadcn/ui로 구성해 빠르고 깔끔하게 UI 완성 |
| 드래그 앤 드롭 | dnd-kit | 활발히 유지보수되는 React DnD 라이브러리, 접근성 지원 |
| 백엔드/DB | Supabase (Postgres + Auth) | 무료 티어로 DB + 로그인 인증까지 한 번에 해결 |
| 인증 | Supabase Auth (이메일/비밀번호 또는 매직링크) | 1인용이라 최소한의 로그인만 필요 |
| 호스팅 | Vercel (Hobby 무료 플랜) | Next.js 배포 표준, 개인 프로젝트 트래픽엔 무료로 충분 |

**비용**: Vercel Hobby(무료) + Supabase Free tier(무료, DB 500MB / 월간 API 요청 한도 넉넉함) 조합으로 개인용은 완전 무료로 운영 가능합니다.

## 5. 데이터 모델 (초안)
`todos` 테이블
| 필드 | 타입 | 설명 |
|---|---|---|
| id | uuid | PK |
| user_id | uuid | Supabase Auth 사용자 (RLS로 본인 데이터만 접근) |
| content | text | 할 일 내용 |
| day | text (nullable) | `null`(Todo List 전역 보관함) 또는 `mon`~`sun` |
| week_start_date | date (nullable) | `day`가 `mon`~`sun`일 때만 값 있음 — 해당 요일이 속한 주의 월요일 날짜. 이 값으로 "몇 주차인지" 계산 및 필터링 |
| completed | boolean | 완료 여부 (기본 false) |
| position | int | 같은 칸 내 정렬 순서 |
| created_at | timestamptz | 생성 시각 |

- 주차 네비게이션에서 보여줄 "52주" 같은 주차 번호는 `week_start_date`로부터 ISO 주차를 계산해서 표시(별도 저장 안 함).
- Row Level Security로 `user_id = auth.uid()` 조건을 걸어 본인 데이터만 읽기/쓰기 가능하도록 구성합니다.

## 6. 향후 확장 아이디어 (MVP 이후, 지금은 안 함)
- ~~시간 단위 입력 및 표시~~ — **완료 (2026-09-11)**. Mon~Sun이 시간축 있는 캘린더 그리드로 바뀌었고,
  할 일을 드래그로 특정 시간에 배치 + 블록 리사이즈로 소요 시간 조절 가능. 자세한 내용은
  [HANDOFF.md](./HANDOFF.md)의 결정 흐름 8번 참고.
- 카테고리/색상 태그 (콘텐츠·회사·루틴·개인 등)
- 월간/분기 플래너 탭 확장
- ~~"이번 주로 돌아가기" 바로가기 버튼~~ — 완료 (주차 네비게이션의 "이번 주" 버튼)
- 같은 시간대에 여러 할 일이 겹칠 때 나란히 배치하는 레이아웃 (지금은 겹쳐서 보임)

## 7. 진행 순서
1. ✅ 기획서 확정
2. ✅ 프로젝트 초기 세팅 (Next.js + Tailwind + dnd-kit)
3. ✅ 정적 UI 레이아웃 구현 (8칸 보드)
4. ✅ Todo 추가/수정/삭제 (로컬 상태 → Supabase로 교체 완료)
5. ✅ 드래그 앤 드롭 구현
6. ✅ Supabase 연동 (이메일/비밀번호 로그인 + 데이터 저장/실시간 동기화, RLS 적용) — 설정 방법은 [README.md](./README.md#supabase-설정-최초-1회) 참고
7. ✅ Vercel 배포 — https://plan0.vercel.app

## 8. PARA 확장 기획 (Project / Area / Resource) — (2026-09-12)

> **구현 완료 (2026-09-12).** 개념(8.1~8.3) → HTML 목업 컨펌(왼쪽 아이콘 레일 + 오른쪽 기존
> Todo List 재사용 구조로 확정) → 코드 반영까지 끝남. 자세한 구현 메모는 HANDOFF.md 11번 참고.
> 아직 실제 Supabase 프로젝트에서 동작 확인은 안 됨 — HANDOFF.md "아직 안 끝난 것" 9번 참고.

### 8.1 목적

지금까지의 "요일·시간 캘린더"는 **언제 할지**를 답하는 축입니다. 여기에 **무엇을 위해 하는지**를 답하는
두 번째 축으로 [PARA](https://fortelabs.com/blog/para/) (Project / Area / Resource / Archive) 방법론을
얹습니다. 두 축은 서로 독립적으로 동작합니다 — 어떤 할 일이 특정 프로젝트에 속해 있어도, 요일/시간
캘린더 배치는 그것과 무관하게 계속 할 수 있고 그 반대도 마찬가지입니다. (확정)

### 8.2 이 앱에서의 PARA 개념 정의

**Project / Area / Resource는 계층 구조가 아니라 완전히 독립된 3개의 병렬 분류 체계입니다.**
("Area가 Project를 묶는다"는 이전 초안의 가정은 폐기 — 사용자 확인.) 셋 다 "이름을 가진 여러 개의
컨테이너를 만들 수 있고, 그 컨테이너에 할 일을 드래그해서 매핑한다"는 동일한 동작 방식을 공유하되,
의미만 다릅니다:

- **Project(프로젝트)**: **시작과 끝이 있는 일.** 예: "6월 한 달 동안 영어 강의 완강". 상태는 `진행중` /
  `완료` 정도로 최소화 (세분화는 향후 확장).
- **Area(영역)**: **시작과 끝이 없이 계속 해야 하는 일들.** 예: "영어". Project와 동일하게 할 일을
  직접 드래그해서 매핑받습니다 (확정).
- **Resource(리소스)**: **지금 당장은 신경 쓰지 않지만 관심 있는 것들의 모음.** 예: "일본어" (지금
  집중하는 건 아니지만 관심은 있는 것). Project/Area와 동일하게 할 일을 드래그해서 보낼 수 있습니다
  (확정). 기존 `/api/clip` 스크랩 기능(현재는 Todo List로 들어감)과 개념적으로 겹치는 지점이 있어
  향후 연결을 고려할 수 있습니다 (열린 질문 참고).

  > 같은 "언어 공부"라는 주제도 지금 기간을 정해 몰입 중이면 Project("영어 강의 완강"), 꾸준히
  > 유지만 하면 되는 거면 Area("영어"), 아직 시작 안 했지만 관심만 있으면 Resource("일본어")로
  > 갈리는 식입니다 — 주제가 아니라 **지금 이 일에 쏟는 주의/시간의 성격**이 분류 기준입니다.

- **컨테이너(바구니/폴더) 비유**: Project/Area/Resource 각각은 폴더나 바구니 같은 것이라, 인스턴스
  하나(예: Area "영어")에 할 일을 몇 개든 담을 수 있습니다(0..N, 이미 데이터 모델에 반영됨 — 8.4 참고).
  다만 할 일 하나는 여러 바구니에 동시에 담기지 않고 최대 1곳에만 들어갑니다(8.3 확정 사항, 변경 없음).
- **Archive**: 이번 기획 범위에서 제외합니다. Project/Area/Resource는 "완료"/"보관" 정도의 상태만
  갖고, 별도의 보관함 화면은 나중에 필요해지면 추가합니다.

### 8.3 관계 구조 (확정)

```
Project (이름을 가진 컨테이너, 0..N개)
Area    (이름을 가진 컨테이너, 0..N개)     ── 서로 완전히 독립, 계층 관계 없음
Resource(이름을 가진 컨테이너, 0..N개)

Todo ──드래그로 매핑── 위 세 종류 중 구체적인 컨테이너 인스턴스 하나
```

- Project, Area, Resource는 서로 완전히 독립적인 top-level 목록입니다. 하나가 다른 하나를 담거나
  소속되는 관계는 없습니다.
- 할 일 1개는 이 셋(정확히는 셋 중 하나의 구체적 인스턴스)을 통틀어 **최대 1곳에만** 속합니다.
  둘 이상 동시에 속하지 않습니다 — 예를 들어 Project "이사 준비"와 Area "건강"에 동시에 매핑될 수
  없습니다. 아무 데도 안 속하면 기존처럼 "미배정" 상태(Todo List 보관함에만 존재).
- 요일/시간 캘린더 배치는 이 분류와 완전히 독립적인 축으로, 변경 없이 계속 별개로 동작합니다.

### 8.4 데이터 모델 (구현 완료 — `supabase/schema.sql`)

새 테이블 (세 테이블은 같은 모양, 계층 관계 없음):

| 테이블 | 필드 | 설명 |
|---|---|---|
| `projects` | id, user_id, name, status (`active`\|`completed`), start_date, due_date (nullable), notes (nullable), created_at, completed_at | 프로젝트 |
| `areas` | id, user_id, name, archived (bool), notes (nullable), created_at | 영역 — `due_date` 없음 (확정) |
| `resources` | id, user_id, name, archived (bool), notes (nullable), created_at | 리소스 — `due_date` 없음 (확정) |

- `notes`는 구현 단계에서 추가 — 8.5.1의 Notes 탭 내용을 저장할 곳이 필요해서 세 테이블 모두에 넣음
  (기획 초안에는 없었지만 Notes 탭이 자유 텍스트를 보여주려면 저장할 컬럼이 있어야 해서 자연스럽게 추가).

- `start_date`/`due_date`는 **Project 테이블에만** 있습니다 — Area/Resource는 끝(마감)이 없는
  개념이라 `due_date`를 두지 않습니다 (확정).
- `start_date`는 `created_at`(레코드 생성 시각)과 **별개의 필드**입니다 — 사용자가 직접 지정하는
  "이 프로젝트를 실제로 시작한 날짜"이고, `created_at`은 그냥 "앱에서 이 프로젝트를 만든 시각"이라
  다를 수 있습니다 (확정).

기존 `todos` 테이블에 컬럼 추가:

| 필드 | 타입 | 설명 |
|---|---|---|
| `project_id` | uuid, nullable FK→projects | |
| `area_id` | uuid, nullable FK→areas | |
| `resource_id` | uuid, nullable FK→resources | |

- 셋 중 최대 1개만 값이 있어야 합니다 — DB에 `CHECK (num_nonnulls(project_id, area_id, resource_id) <= 1)`
  제약을 걸어서 강제합니다 (Postgres `num_nonnulls` 함수 사용).
- `day`/`week_start_date`/`start_minutes`/`duration_minutes`는 그대로 둡니다 — PARA 축과 무관하게
  유지 (확정).

### 8.5 화면/UX 설계 (구현 완료 — `/para`)

- **완전히 별도의 화면**(`/para`)으로 구현. 메인 캘린더 화면 아이콘 레일에 아이콘 추가해서 이동.
- (2026-09-12, 사용자 손그림 스케치로 수정) 상단 탭 대신 **왼쪽에 Project/Area/Resource 아이콘
  레일**로 종류를 전환하고, 중앙에 그 종류의 컨테이너 카드 목록이 전체 폭으로 보임.
- **오른쪽 "할 일 보관함"은 새로 만들지 않고 기존 `IconRail`/`TodoPanel`을 그대로 재사용** —
  메인 캘린더 화면과 똑같은 아이콘/슬라이드 패널(사용자 지시: "오른쪽은 기존 투두리스트 유지").
  이미 매핑된 항목은 어느 컨테이너에 속해 있는지 작은 배지로 표시(`TodoCard`의 `badge` prop).
  - 할 일 카드를 특정 컨테이너 카드 위로 드래그하면 그 컨테이너로 매핑(`project_id`/`area_id`/
    `resource_id` 중 해당 필드 설정, 나머지 둘은 자동으로 비움). 다시 보관함으로 드래그하면 매핑 해제.
- 컨테이너(Project/Area/Resource)를 클릭하면 상세 화면(`/para/[kind]/[id]`)으로 이동.
- Project/Area/Resource 생성은 각각 이름 입력 폼 하나로 최소화 (기존 "할 일 추가" 폼과 비슷한 패턴).
- 메인 캘린더 화면의 할 일 카드/블록에는 이번엔 배지를 안 붙임 (범위 밖으로 남김 — 8.7 참고).

#### 8.5.1 Project 상세 화면 스펙 (레퍼런스: Notion 프로젝트 페이지 캡처, 2026-09-12 — 확정)

사용자가 첨부한 Notion 프로젝트 페이지 캡처를 레퍼런스로 **Project 상세 화면**을 확정했고,
Area/Resource 상세 화면도 같은 골격을 공유합니다.

**Project 상세 화면 구성 (위에서 아래 순서):**

1. **헤더**: 아이콘(이모지 1개, 레퍼런스의 🚀처럼) + 제목(프로젝트 이름)
2. **요약 속성 줄**: `Status`(진행중/완료) · `Due date` · `Progress`(이 프로젝트에 속한 할 일 완료
   비율 %, 자동 계산 — 저장 안 함). **`Priority`는 제외.** `Progress`는 **Project 전용 개념**이고
   요약 줄에만 표시합니다(Overview Properties에는 넣지 않음 — 중복 표시 안 함).
3. **탭**: **Overview / Tasks / Notes** 3개만 (`Ideas`, `Meeting Notes`는 제외)
   - **Overview 탭**: 라벨-값 형태의 세로 Properties 리스트 —
     - `Start date` (프로젝트 시작일, `created_at`과 별개로 저장/표시)
     - `Completion date` = 기존 데이터 모델의 `completed_at` (완료 전엔 "Empty"로 표시)
     - `Days left` (`Due date` 기준 자동 계산)
     - 레퍼런스의 `Type`, "12 more properties" 접기/펼치기, Properties 안의 별도 `Notes` 줄은 제외
       (Notes는 3번 탭으로 이미 분리했고, 이 앱 필드 수가 적어 접기/펼치기도 불필요)
   - **Tasks 탭**: 이 프로젝트에 매핑된 할 일 목록. **기존 앱의 Todo 카드 스타일 리스트**를 그대로
     사용 (레퍼런스의 표(Table) 형태는 채택 안 함).
   - **Notes 탭**: 프로젝트 전체에 대한 자유 텍스트 메모 (개별 할 일의 메모와는 별개).

**Area/Resource 상세 화면**: 같은 헤더 + Overview/Tasks/Notes 탭 구조를 쓰되,

- 요약 속성 줄에는 `Due date`/`Progress`가 없습니다(둘 다 Project 전용 개념) — `Status` 정도만 남습니다.
- Overview 탭 Properties에는 `Start date`만 있고, `Completion date`/`Days left`는 없습니다
  (Area/Resource는 끝이 없는 개념이라 완료/마감 관련 필드 자체가 없음).
- Tasks 탭, Notes 탭은 Project와 동일한 방식.

### 8.6 진행 순서 — 전부 완료 (2026-09-12)

1. ✅ 데이터 모델 + `supabase/schema.sql`에 `projects`/`areas`/`resources` 테이블과 `todos` 컬럼(3개
   nullable FK + CHECK 제약) 추가, RLS 정책 작성
2. ✅ `/para` 라우트 + 왼쪽 아이콘 레일 + Project/Area/Resource 목록 UI
3. ✅ Project/Area/Resource 생성 폼
4. ✅ 할 일 보관함 ↔ 컨테이너 카드 드래그 앤 드롭 매핑 (dnd-kit 재사용, 셋 중 하나로만 배타적으로 매핑)
5. ✅ 컨테이너 상세 화면 (`/para/[kind]/[id]`, Overview/Tasks/Notes)
6. ⬜ (선택, 범위 밖으로 보류) 메인 캘린더 화면에 소속 컨테이너 배지 표시

### 8.7 열린 질문 — 정리 (2026-09-12)

1. **완료/보관 처리**: 목록에서 안 사라지고 상태 배지만 바뀜(필터링/접기 없음) — 다음 할 일로 남김
   (HANDOFF.md "아직 안 끝난 것" 9번).
2. **`/api/clip` ↔ Resource 연결**: 이번엔 안 함, 완전히 별개로 남겨둠 — 다음에 필요해지면 논의.
3. **메인 캘린더 화면 배지**: 이번엔 안 붙임(8.6의 6번과 동일 — 범위 밖).
4. **탭 vs 3열**: 사용자가 손그림 스케치로 준 **왼쪽 아이콘 레일** 방식으로 대체 확정 (8.5 참고,
   탭/3열 둘 다 채택 안 함).

## 9. 노트/자료 확장 기획 (Google Drive 연동) — (2026-09-18, 논의 중 · 미구현)

> **아직 코드 반영 전 초안**입니다. DESIGN.md 8번 체크리스트("레이아웃이 걸린 작업이면 먼저
> 캔버스에 시안 그려서 컨펌받기")에 따라, 9.5 화면/UX는 목업 컨펌 전까지 확정이 아닙니다.

### 9.1 배경 / 문제

- 기존에는 Obsidian(마크다운 파일 + 애플 생태계 동기화)으로 메모를 관리했고, 메모 작성 경험 자체엔
  만족했음. 다만 (a) 회사가 Windows 환경이라 Obsidian을 쓰기 어렵고, (b) Obsidian은 할 일을
  캘린더로 모아보는 데는 약해서, 이 앱(plan.0)을 별도로 만들어 할 일/PARA 쪽은 이미 해결함.
- 남은 문제: **메모(및 PPT/PDF 같은 자료)도 파일로 관리하고 싶다** — 특정 DB에 종속되지 않고,
  AI(Claude)가 폴더째로 쉽게 읽고 정리할 수 있어야 함. 회사에서는 Obsidian이나 Git 클라이언트
  같은 별도 프로그램을 설치/연동할 수 없어서, **plan.0 웹앱 화면 안에서** 열람/편집/업로드가
  끝나야 함(외부 사이트로 나가는 흐름은 채택 안 함).
- 프로젝트 하나에 노트가 여러 개 묶일 수 있고, 마크다운 메모뿐 아니라 PPT/PDF 같은 첨부/백업
  자료도 자주 쌓임 — 이 둘을 같은 곳에서 관리하고 싶음.

### 9.2 저장소 검토 및 결정

| 후보 | 검토 결과 |
|---|---|
| Supabase Storage | 이미 쓰는 인프라라 연동은 쉽지만, 무료 티어 1GB로 PPT/백업 파일 위주 사용엔 금방 부족함 — 기각 |
| GitHub repo (마크다운 파일) | 마크다운 자체엔 적합(버전관리, Claude가 `add_repo`로 폴더째 읽기 쉬움)하지만, PPT/PDF 같은 바이너리를 넣으면 저장할 때마다 파일 전체가 히스토리에 쌓여 레포가 금방 무거워짐. 또한 "github.dev로 이동해서 편집"하는 흐름은 회사에서 웹앱만 써야 하는 제약과 맞지 않음 — 기각 |
| **Google Drive** | **채택.** 무료 15GB로 용량 문제 해결, 바이너리 파일에 원래 최적화된 서비스(자체 미리보기 뷰어 `webViewLink` 제공), 이 세션에 이미 연결된 Google Drive 커넥터로 **추가 연동 없이 Claude가 바로 검색/읽기 가능** |

> **결정**: 마크다운 노트 + PPT/PDF 등 첨부 자료를 전부 **Google Drive 한 곳**에 저장한다. 종류별로
> 저장소를 나누지 않는다(하나로 통일해서 관리 포인트를 줄임).

### 9.3 폴더 구조 (안)

PARA 구조를 그대로 미러링한다 — 앱의 Project/Area/Resource 목록과 Drive 폴더가 1:1 대응:

```
PARA/
  1-Projects/<프로젝트명>/    ← note.md, 발표자료.pptx, 백업.pdf 등 종류 무관하게 같이 보관
  2-Areas/<영역명>/
  3-Resources/<리소스명>/
```

- 프로젝트/영역/리소스를 앱에서 새로 만들 때, 해당 하위 폴더를 Drive API로 자동 생성한다.
- 이름이 아니라 **Drive 폴더 ID**로 연결한다 (9.4 참고) — 나중에 폴더명을 바꿔도 연결이 깨지지
  않게 하기 위함.

### 9.4 데이터 모델 (안)

기존 `projects`/`areas`/`resources` 테이블에 컬럼 추가:

| 필드 | 타입 | 설명 |
|---|---|---|
| `drive_folder_id` | text, nullable | 이 컨테이너에 대응하는 Google Drive 폴더 ID. 컨테이너 생성 시 자동 발급/저장, 값이 없으면 아직 폴더 미생성 상태로 취급 |

- 기존 8.4에서 만들었던 `notes`(자유 텍스트) 컬럼은 이미 제거된 상태(HANDOFF.md "아직 안 끝난 것"
  9번) — 이번 기획으로 대체되어 되살릴 필요 없음.
- 파일 목록 자체는 DB에 별도로 캐시/미러링하지 않고, 화면을 열 때마다 Drive API로 그 폴더의
  파일 목록을 실시간 조회한다(9.7 열린 질문 1번 참고 — 캐싱은 필요해지면 나중에 추가).

### 9.5 화면/UX 설계 (캔버스 시안 v2 — 컨펌 진행 중)

> 캔버스: https://claude.ai/artifact/KMMXdKodS4dpEbt7iehxpq (Desktop/Mobile 두 아트보드, 인터랙티브)

**"노트"라는 이름이 두 곳에서 쓰이는 문제를 먼저 정리함 (2026-09-18 논의):**
기존 `todos.kind='note'`는 짧은 메모/할일이 아니라 **"스크랩"** — 폰에서 글/영상 등을 볼 때
"나중에 보려고 일단 담아두는" 캡처였음(사용자 표현: "최근 AI 동향 같은 글을 봤을 때 빠르게
스크랩하는 느낌"). 이 중 일부(예: 나중에 요약하거나 다른 스크랩과 엮어서 정리하고 싶은 것)만
**Drive의 정식 마크다운 노트로 "승격"**되고, 나머지(예: 그냥 참고할 운동 루틴 링크)는 승격 없이
스크랩인 채로 남아도 됨 — 즉 스크랩과 Drive 노트는 경쟁 관계가 아니라 **원재료 → (선택적) 완성본**
관계. 그래서 용어와 탭 배치를 다음처럼 분리하기로 함:

- 컨테이너 상세화면 세 번째 탭 이름:~~Notes~~ → **"자료"** (Drive 파일 전용: 마크다운 노트 +
  PPT/PDF/이미지 등 첨부).
- `todos.kind='note'` 항목은 UI에서 **"스크랩"**으로 부르고, 여전히 **Tasks 탭 안에** "할 일" 섹션과
  나란히 "스크랩" 섹션으로 표시(체크박스 없음, 지금 방식 그대로). 인박스 캡처(공유하기 → API)
  흐름과 할일↔스크랩 전환 로직은 전혀 안 바뀜 — 이름과 탭 배치만 바뀜.
- **스크랩 → 노트 승격 플로우** (새 기능): 스크랩 섹션에 "노트로 만들기" 버튼 → 하나 이상의 스크랩을
  체크박스로 선택(여러 개를 하나의 새 노트로 엮는 것도 가능, 사용자가 명시적으로 요청한 유스케이스)
  → "노트 만들기" 클릭 → 선택된 스크랩들의 제목/URL/메모를 섹션별로 엮은 마크다운 초안으로 "자료"
  탭에 새 노트 생성 + 그 자리에서 인앱 에디터로 바로 열림 → **원본 스크랩은 삭제**(사용자 확정,
  승격 후 스크랩을 따로 보관할 필요는 없다고 판단).

탭 안 나머지 구성(시안 v2에 반영됨):
1. **Tasks 탭**: "할 일" 섹션(체크박스) + "스크랩" 섹션(체크박스 없음, clip 아이콘) + 스크랩 선택
   모드/승격 버튼.
2. **자료 탭**: 파일 목록(마크다운/PPT/PDF/이미지 아이콘으로만 구분, 별도 색상 코딩 없음) + 업로드
   버튼 + "새 노트" 버튼. 마크다운 파일 클릭 시 인앱 에디터(제목 입력 + textarea)로 전환, 뒤로가기/저장
   버튼. 비-마크다운 파일은 외부 링크 아이콘만 표시(클릭 시 Drive 자체 미리보기 새 탭 — 별도 컴포넌트
   안 만듦).
3. 승격 직후 에디터 화면에는 "선택한 스크랩으로 새 노트를 만들었고 원본은 정리됐다"는 안내 배너 표시.

**노트 에디터에 옵시디언 Properties 스타일 메타데이터 추가** (2026-09-18, 사용자가 옵시디언
스크린샷으로 레퍼런스 제시): 제목 아래 URL 같은 구조화된 정보를 본문에 텍스트로 섞어 쓰지 않고,
옵시디언의 Properties 패널처럼 **"속성" 섹션**(라벨-값 형태 행 목록 + "속성 추가")으로 분리해서
보여준다. 스크랩 승격 시 각 스크랩의 URL은 `속성.URL`(여러 개면 리스트)로 들어가고, 메모/본문
내용만 마크다운 본문에 남는다. 기술적으로는 이게 옵시디언과 완전히 같은 방식이라 자연스럽게
맞아떨어짐 — 실제 `.md` 파일 저장 형식은 **YAML frontmatter**(`--- key: value ---`)로 저장하고,
인앱 에디터가 그 frontmatter를 파싱해서 속성 UI로 렌더링 + 본문은 별도 textarea로 보여주는
구조가 되어야 함(이 파일을 나중에 옵시디언에서 직접 열어도 Properties로 정상 인식됨). 이 부분은
아직 프론트의 frontmatter 파서/렌더러를 어떻게 구현할지(직접 파싱 vs 라이브러리) 정하지 않음 —
코드 작업 시작할 때 결정.

아직 남은 것: 이 v2 시안 자체의 최종 컨펌(레이아웃/문구), 그리고 실제 코드 반영 시 스크랩 승격
로직을 프론트에서 처리할지(마크다운 조합 후 `/api/drive/notes` POST + `todos` DELETE를 순서대로
호출) 서버에 전용 엔드포인트(`/api/drive/promote` 같은)를 새로 만들지는 코드 작업 시작할 때 결정.

### 9.6 인증/연동 방식 — 구현 완료 (2026-09-18)

- Google Cloud 콘솔에 프로젝트 등록 + OAuth 동의화면 설정 (1인용이라도 Drive API 쓰려면 최초 1회
  필요) — 사용자가 직접 진행해야 하는 부분이라 README.md "Google Drive 연동 설정" 섹션에 단계별로
  정리해둠.
- 서버(Next.js API route)에서 Drive API(`googleapis` 라이브러리)를 호출 — refresh token을 서버
  환경변수(Vercel)에 저장해두고, 앱은 그 토큰으로 서버 사이드에서만 Drive에 접근한다(클라이언트에
  Google 자격증명 노출 안 함). `src/lib/google-drive.ts`.
- **스코프는 `drive.file`(앱이 직접 만든 파일에만 접근)로 최소화** — 애초 안(9.3)은 사용자가
  드라이브에 미리 만들어둔 최상위 폴더 ID를 넘겨받는 방식이었는데, 그러면 그 폴더는 앱이 만든 게
  아니라서 `drive.file` 스코프로는 접근이 안 되고 더 넓은(민감한) `drive` 스코프가 필요해짐. 대신
  **최상위 "PARA" 폴더 자체도 앱이 최초 호출 시 내 드라이브(`root`)에 자동 생성**하도록 바꿔서,
  전체 트리가 앱 소유가 되어 `drive.file` 스코프만으로 충분하게 만듦 — 사용자가 폴더를 직접 만들
  필요도, `GOOGLE_DRIVE_ROOT_FOLDER_ID` 환경변수도 없어짐.
- 구현된 API: `POST /api/drive/folder`(컨테이너 → Drive 폴더 조회/생성 + `drive_folder_id` 저장),
  `GET /api/drive/files`(폴더 내 파일 목록), `POST /api/drive/files`(바이너리 업로드),
  `POST /api/drive/notes`(마크다운 파일 생성), `GET/PUT /api/drive/notes`(내용 읽기/쓰기).

### 9.7 진행 순서

1. ⬜ Google Cloud 프로젝트/OAuth 동의화면 등록, refresh token 발급 및 환경변수 등록 — **사용자가
   직접 해야 하는 부분** (README.md 참고), 아직 미완료. 이게 끝나야 아래 구현된 API들을 실제로
   테스트해볼 수 있음.
2. ✅ `supabase/schema.sql`에 `drive_folder_id` 컬럼 추가 (projects/areas/resources 3개 테이블)
3. ✅ 서버 API route: 폴더 생성/조회, 파일 목록, 업로드, 마크다운 파일 읽기/쓰기 (9.6 참고)
4. ⬜ 컨테이너 생성 플로우에 Drive 폴더 자동 생성 연결 — 지금은 `/api/drive/folder`를 호출하는
   쪽이 아직 없음(lazy 생성 방식으로, 9.8 열린 질문 4번 결정에 따라 파일 탭을 처음 열 때 호출하는
   게 유력)
5. ⬜ 상세 화면 Notes 탭 UI 교체 (파일 목록 + 업로드 버튼 + 인앱 마크다운 에디터) — 착수 전 캔버스
   시안 컨펌 필요 (DESIGN.md 체크리스트 1번)
6. ⬜ 기존에 이미 만들어둔 프로젝트/영역/리소스(폴더 미생성 상태)에 대한 소급 처리 방식 결정 (9.8
   열린 질문 4번)

### 9.8 열린 질문

1. **파일 목록 캐싱 여부**: 매번 Drive API를 실시간 호출할지, 아니면 목록을 DB에 캐시해두고
   주기적/이벤트 기반으로 갱신할지 — 초기엔 실시간 호출로 시작하고 느려지면 캐싱 고려.
2. **마크다운 인앱 에디터 범위**: 문법 하이라이팅/미리보기까지 넣을지, 순수 textarea로 시작할지.
3. **업로드 용량/형식 제한**: 특별히 제한을 둘지, Drive API 기본 한계(파일당 5TB 등)를 그대로
   따를지 — 실사용 패턴 보고 결정.
4. **기존 컨테이너 소급 처리**: 이미 만들어져 있는 Project/Area/Resource는 `drive_folder_id`가
   비어있는데, 처음 파일 탭을 열 때 그 자리에서 폴더를 생성(lazy)할지, 마이그레이션 스크립트로
   한 번에 만들지.
5. **Archive된 컨테이너의 Drive 폴더**: 완료/보관 처리된 프로젝트의 폴더를 그대로 둘지, Drive 안에서도
   별도 보관 폴더로 옮길지 — 8.7의 "완료/보관 처리" 열린 질문과 연결되는 사안이라 같이 정리 필요.
